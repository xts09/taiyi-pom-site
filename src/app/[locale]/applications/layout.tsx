import type { ReactNode } from "react";
import "../../(en)/styles/applications.css";

export default function LocalizedApplicationsLayout({
  children,
}: Readonly<{ children: ReactNode }>) {
  return children;
}
