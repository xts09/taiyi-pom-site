import type { ReactNode } from "react";
import "../../(en)/styles/resources.css";

export default function LocalizedNewsLayout({
  children,
}: Readonly<{ children: ReactNode }>) {
  return children;
}
