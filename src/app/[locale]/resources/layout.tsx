import type { ReactNode } from "react";
import "../../(en)/styles/resources.css";

export default function LocalizedResourcesLayout({
  children,
}: Readonly<{ children: ReactNode }>) {
  return children;
}
