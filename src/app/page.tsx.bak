import Link from "next/link";
import { products } from "@/data/products";

export default function Home() {
  return (
    <main className="min-h-screen bg-slate-50 text-slate-900">
      <section className="mx-auto max-w-6xl px-6 py-20">
        <div className="mb-16 max-w-3xl">
          <p className="mb-4 text-sm font-semibold uppercase tracking-wide text-blue-700">
            Jiangsu Taiyi Nano Technology Co., Ltd.
          </p>

          <h1 className="mb-6 text-4xl font-bold tracking-tight text-slate-950 md:text-6xl">
            POM Resin & Modified POM Compounds Manufacturer in China
          </h1>

          <p className="mb-8 text-lg leading-8 text-slate-700">
            We focus on high-performance POM materials for injection molding
            applications, including wear-resistant, low-friction, glass-fiber
            reinforced, and conductive / antistatic POM compounds.
          </p>

          <div className="flex flex-wrap gap-4">
            <Link
              href="/products"
              className="rounded-lg bg-blue-700 px-6 py-3 text-sm font-semibold text-white hover:bg-blue-800"
            >
              View Products
            </Link>

            <Link
              href="/contact"
              className="rounded-lg border border-slate-300 px-6 py-3 text-sm font-semibold text-slate-900 hover:bg-white"
            >
              Contact Sales
            </Link>
          </div>
        </div>

        <div className="mb-8 flex items-end justify-between gap-6">
          <div>
            <p className="mb-2 text-sm font-semibold uppercase tracking-wide text-blue-700">
              Product Directions
            </p>
            <h2 className="text-3xl font-bold tracking-tight text-slate-950">
              Main POM Materials
            </h2>
          </div>

          <Link
            href="/products"
            className="hidden text-sm font-semibold text-blue-700 hover:text-blue-800 md:block"
          >
            View all products →
          </Link>
        </div>

        <div id="products" className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">
          {products.slice(0, 6).map((product) => (
            <Link
              key={product.slug}
              href={`/products/${product.slug}`}
              className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm transition hover:-translate-y-1 hover:shadow-md"
            >
              <p className="mb-2 text-xs font-semibold uppercase tracking-wide text-blue-700">
                {product.category}
              </p>

              <h3 className="mb-3 text-lg font-semibold text-slate-950">
                {product.title}
              </h3>

              <div className="mb-4 grid grid-cols-2 gap-3 text-sm text-slate-700">
                <div>
                  <span className="font-medium text-slate-950">Grade:</span>{" "}
                  {product.grade}
                </div>

                <div>
                  <span className="font-medium text-slate-950">MFI:</span>{" "}
                  {product.mfi}
                </div>

                <div>
                  <span className="font-medium text-slate-950">Color:</span>{" "}
                  {product.color}
                </div>

                <div>
                  <span className="font-medium text-slate-950">Docs:</span>{" "}
                  {product.documents.join(", ")}
                </div>
              </div>

              <p className="text-sm leading-6 text-slate-600">
                Typical applications: {product.applications.join(", ")}.
              </p>
            </Link>
          ))}
        </div>

        <section className="mt-16 grid gap-6 md:grid-cols-3">
          <div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
            <h3 className="mb-3 text-lg font-semibold text-slate-950">
              Application-Oriented Selection
            </h3>
            <p className="text-sm leading-6 text-slate-600">
              Material recommendations can be made based on application,
              processing method, friction, wear resistance, stiffness,
              dimensional stability, and volume requirements.
            </p>
          </div>

          <div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
            <h3 className="mb-3 text-lg font-semibold text-slate-950">
              Main Applications
            </h3>
            <p className="text-sm leading-6 text-slate-600">
              Precision gears, sliding parts, bushings, automotive functional
              parts, electronic components, and other injection molded parts.
            </p>
          </div>

          <div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
            <h3 className="mb-3 text-lg font-semibold text-slate-950">
              Available Documents
            </h3>
            <p className="text-sm leading-6 text-slate-600">
              TDS, SDS, REACH, RoHS, IATF 16949 and ISO system documents are
              available upon request.
            </p>
          </div>
        </section>

        <section className="mt-16 rounded-2xl bg-slate-950 p-8 text-white shadow-sm md:flex md:items-center md:justify-between">
          <div className="max-w-2xl">
            <h2 className="text-2xl font-bold tracking-tight">
              Need a POM material recommendation?
            </h2>

            <p className="mt-3 text-sm leading-6 text-slate-300">
              Share your application, key performance requirements, current
              reference material, and estimated volume. We will recommend a
              suitable POM material direction for review.
            </p>
          </div>

          <Link
            href="/contact"
            className="mt-6 inline-flex rounded-lg bg-blue-600 px-6 py-3 text-sm font-semibold text-white hover:bg-blue-700 md:mt-0"
          >
            Contact Sales
          </Link>
        </section>
      </section>
    </main>
  );
}